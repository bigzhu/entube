query ArticleItems($offset: Int = 0, $limit: Int = 10) {
  articles(offset: $offset, limit: $limit) {
    id
    favicon
    created_by
    created_at
    thumbnail
    title
    updated_at
    url
    updated_by
  }
}
